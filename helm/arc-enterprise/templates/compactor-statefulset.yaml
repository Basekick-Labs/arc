{{- if .Values.compactor.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "arc-enterprise.compactorName" . }}
  labels:
    {{- include "arc-enterprise.roleLabels" (dict "ctx" . "role" "compactor") | nindent 4 }}
spec:
  serviceName: {{ include "arc-enterprise.compactorHeadlessName" . }}
  replicas: {{ .Values.compactor.replicas }}
  selector:
    matchLabels:
      {{- include "arc-enterprise.roleSelectorLabels" (dict "ctx" . "role" "compactor") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "arc-enterprise.roleLabels" (dict "ctx" . "role" "compactor") | nindent 8 }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "arc-enterprise.serviceAccountName" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: arc
          image: {{ include "arc-enterprise.image" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: 8000
            - name: coordinator
              containerPort: 9100
            - name: raft
              containerPort: 9200
          env:
            {{- include "arc-enterprise.commonClusterEnv" . | nindent 12 }}
            {{- include "arc-enterprise.raftEnv" . | nindent 12 }}
            - name: ARC_CLUSTER_ROLE
              value: "compactor"
            - name: ARC_CLUSTER_ADVERTISE_ADDR
              value: "$(POD_NAME).{{ include "arc-enterprise.compactorHeadlessName" . }}.{{ .Release.Namespace }}.svc.cluster.local:9100"
            # The compactor joins Raft like every other role. Without an
            # advertisable Raft address the transport refuses the address
            # ("local bind address is not advertisable"), Coordinator.Start()
            # fails, and main.go logs "Failed to start cluster coordinator -
            # running in standalone mode" and carries on — so the pod looks
            # healthy while being invisible to the cluster: not in any
            # registry, never eligible for the compactor lease, and, worst,
            # still compacting the shared bucket on its own alongside whoever
            # does hold the lease. The same gap was fixed for readers and
            # missed here until #870. scripts/test_cluster_raft_env.sh now
            # asserts it for every role.
            - name: ARC_CLUSTER_RAFT_ADVERTISE_ADDR
              value: "$(POD_NAME).{{ include "arc-enterprise.compactorHeadlessName" . }}.{{ .Release.Namespace }}.svc.cluster.local:9200"
            - name: ARC_CLUSTER_SEEDS
              value: {{ include "arc-enterprise.writerSeeds" . | quote }}
            - name: ARC_COMPACTION_ENABLED
              value: "true"
            {{- include "arc-enterprise.storageEnv" . | nindent 12 }}
            {{- with .Values.compactor.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: data
              mountPath: /app/data
            {{- if .Values.cluster.tls.enabled }}
            - name: tls
              mountPath: /etc/arc/tls
              readOnly: true
            {{- end }}
          startupProbe:
            # Boot can legitimately take minutes: the license phase retries a
            # transient license-server outage (bounded 45s) and multi-writer
            # WAL recovery replays before the listener binds — during which
            # /health is connection-refused. Without this gate the liveness
            # probe (kill at ~60s) would murder booting pods, converting a
            # slow boot into a crash-loop. 30 * 10s = 5 minutes of grace;
            # liveness takes over only after the first success.
            httpGet:
              path: /health
              port: 8000
            failureThreshold: {{ .Values.compactor.startupProbeFailureThreshold | default 30 }}
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.compactor.resources | nindent 12 }}
      {{- if .Values.cluster.tls.enabled }}
      volumes:
        - name: tls
          secret:
            secretName: {{ .Values.cluster.tls.existingSecret | quote }}
      {{- end }}
      {{- include "arc-enterprise.roleScheduling" (dict "ctx" . "role" "compactor") | nindent 6 }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        {{- $sc := .Values.compactor.persistence.storageClass | default .Values.storage.local.storageClass }}
        {{- if $sc }}
        storageClassName: {{ $sc | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.compactor.persistence.size | quote }}
{{- end }}
